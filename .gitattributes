# 참조 자산(.txt)은 줄바꿈 변환을 하지 않는다.
#
# 이유: backend/data/lawd_codes.txt 는 정부에서 받은 원본을 그대로 두는 파일이고
# 인코딩이 cp949다. git이 CRLF↔LF를 자동 변환하면 원본과 바이트가 달라져,
# "받은 그대로인가"를 확인할 수 없게 된다(README 부록의 행 수·인코딩 기록이 무의미해짐).
*.txt -text
