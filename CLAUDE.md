# 전세AI프 (jeonse-ai)

> Claude Code가 매 세션 가장 먼저 읽는 프로젝트 안내 문서입니다.

## 1. 서비스 개요

- **서비스명**: 전세AI프 (영문 레포/폴더명: `jeonse-ai`)
- **목적**: 전세 계약을 맺기 **전에**, 등기부등본을 분석해 **깡통전세·전세사기 위험**을 탐지하고, 부동산에 무지한 일반인도 이해할 수 있게 **쉬운 말로 설명**해주는 서비스
- **타깃 사용자**: 부동산 지식이 거의 없는 사람 (사회초년생, 첫 전세 계약자 등)
  - 따라서 결과 화면은 **전문용어 없이**, **결론(위험등급)을 먼저 크게** 보여주고, 근거는 그 아래에서 **펼쳐 보는(접힘/펼침)** 방식으로 제공한다.

## 2. 시스템 구성

- **플랫폼**: Flutter 앱(프론트엔드) + FastAPI(백엔드)
  - **백엔드(FastAPI)**: OCR, 위험 판단, (향후) RAG 담당
  - **앱(Flutter)**: 입력(등기부등본 이미지)과 결과 표시 담당
- **입력 방식 (현재 단계)**: 사용자가 실물 등기부등본(주민센터/인터넷등기소 출력물)을 **카메라로 촬영**하거나 **이미지로 업로드** → OCR로 텍스트 추출
- **OCR 엔진**: **Upstage Document Parse**
- **리포트 생성 (향후)**: **Upstage Solar Pro**

## 3. 핵심 원칙 (반드시 준수)

### 3.1 위험 판단은 객관적 출처 기반 — 규칙 기반 + RAG, ML 분류 아님
- 위험 판단은 머신러닝 **분류 모델이 아니라** **규칙 기반(rule-based)** + (향후) **RAG**로 한다.
  - 이유: 신뢰할 만한 라벨 데이터가 없고, 무엇보다 **판단 근거를 사용자에게 설명할 수 있어야** 하기 때문.
- 모든 **위험 가중치·임계값**은 팀의 추측이 아니라 **권위 있는 출처**에 근거해야 한다.
  - 권위 있는 출처 예: **HUG 공식 기준**, 정부/금융기관의 **전세가율·선순위채권비율 기준**, **판례**.
- 채택한 모든 기준은 **반드시 `docs/decisions.md`에 출처와 함께 기록**한다. 출처 없는 임의 수치 사용 금지.

### 3.2 보수적 편향 — 경고 우선
- **false negative(위험한데 안전하다고 잘못 판단)** 가 **false positive(안전한데 위험하다고 판단)** 보다 훨씬 치명적이다.
- 따라서 시스템은 **안전을 단언하기보다 경고 쪽으로 보수적으로 편향**되어야 한다.

### 3.3 대회 제약 — 국내(한국) AI 모델만 사용
- 본 대회는 **국내(한국) AI 모델만** 사용 가능하다.
- Upstage(**Document Parse** + **Solar Pro**)를 사용하면 이 요건을 충족한다.

## 4. 향후 과제 (지금은 구현하지 않음, 기록만)

- **CODEF API 연동** (주소 입력 → 등기부 자동조회): 최종 제출 버전의 목표지만, 현재는 난이도 때문에 보류. **이미지 OCR을 먼저 완성한 뒤** 추가 예정.
- **RAG**: 판례·법령 기반 설명 생성.
- **국토부 실거래가 API 연동**: 전세가율 계산용 시세 확보.
- **안심전세포털 악성임대인 명단 대조**: 약 1,177건 명단과 대조.

## 5. 폴더 구조

```
jeonse-ai/
├── backend/                  # FastAPI 백엔드 (OCR·위험판단·향후 RAG)
│   └── app/
│       └── main.py           # 헬스체크 엔드포인트 (GET / → {"status":"ok"})
├── frontend/                 # Flutter 앱 (스캐폴딩은 STEP 1에서)
├── docs/
│   └── decisions.md          # 아키텍처 결정 로그 (append-only)
├── .claude/
│   ├── rules/
│   │   ├── risk-scoring.md    # 위험 점수 규칙
│   │   └── api-design.md      # 백엔드 규칙
│   └── commands/
│       └── add-decision.md    # 결정 한 줄 추가 슬래시 명령
├── CLAUDE.md                 # 이 문서
├── CLAUDE.local.md           # 개인 메모 (gitignore)
├── .gitignore
└── README.md
```

## 6. 작업 규칙

- 위험 점수 로직을 다룰 때는 `.claude/rules/risk-scoring.md`를 따른다.
- 백엔드를 다룰 때는 `.claude/rules/api-design.md`를 따른다.
- 새로운 아키텍처 결정을 내리면 `/add-decision` 명령으로 `docs/decisions.md`에 기록한다.
- 모든 문서·주석·커밋 메시지는 **한국어**로 작성한다.
