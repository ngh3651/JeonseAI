# 전세AI프 (JeonseAI)

> Claude Code가 매 세션 가장 먼저 읽는 프로젝트 안내 문서입니다.
> **현재 Phase: E 진행 중 — E-2(Solar 설명+질문 템플릿) 종료 게이트 통과, E-3(판례 큐레이션 매칭) 대기 (세부는 docs/plan.md)** ← Phase 진행에 따라 이 줄을 갱신한다.

## 1. 서비스 한 줄

전세 계약을 맺기 **전에** 등기부등본을 분석해 **깡통전세·전세사기 위험**을 탐지하고,
부동산 비전문가에게 **법적 근거와 함께 쉬운 말로** 설명해주는 앱.

- **대회 맥락**: AI ROOKIE 본선. 최종 평가는 **영상 평가**라 실배포·실회원 가입은 불필요하다.
  단, **시연 자체는 조작된 가짜 데이터가 아니라 실제 등기부등본을 입력해 분석이 실제로
  동작하는 모습**을 보여줄 수 있어야 한다 — 심사위원에게 보여줄 것은 **완성도 높은 시연
  흐름**이지, 겉모습만 갖춘 무대 세트가 아니다.
- **타깃 사용자**: 부동산 지식이 거의 없는 사람 (사회초년생, 첫 전세 계약자, 바쁜 신혼부부).
  결과 화면은 **전문용어 없이, 결론(위험등급)을 먼저 크게**, 근거는 아래에서 **펼쳐 보는** 방식.

## 2. 팀 상황

- 팀원 **5명**, 그중 **개발자 2명** — 담당 폴더를 나누기보다 **같은 코드베이스를 시간대를
  나눠 함께 개발**한다 (충돌 최소화).
- 비개발 팀원 3명:
  - **정민재**: 전세사기 유형 연구, 법령·판례 자료 수집
  - **최가은**: 부동산 중개인 현장 인터뷰
  - **김시원**: 유사 서비스 분석, 타깃 유저 페르소나 관점
  - 이들이 채워 넣을 부분(판례 목록, 위험 기준 출처 등)은 **코드에 박아 넣지 말고
    문서·데이터 파일로 분리**해서, 비개발 팀원이 코드를 몰라도 직접 채워 넣고
    기여할 수 있게 한다.
- 브랜치: 평소 `dev`, Phase 완료 확인 후 `main` 병합 (상세 절차는 README.md)

## 3. 불변 원칙 (반드시 준수)

1. **위험 판단 = 규칙 엔진** (ML 분류 금지). **LLM은 설명·문장 생성만** 한다 —
   LLM이 판정 결과를 바꾸거나 새 판정을 만들 수 없다. (우리 식 가드레일)
2. 모든 **가중치·임계값**은 `권위 출처 확보 → docs/decisions.md 기록 → 코드 반영` 순서로만.
   인정 출처: HUG 공식 기준, 정부/금융기관 기준, 판례, 법령. **출처 없는 수치 금지.**
3. **보수적 편향**: 미탐(false negative, 위험을 놓침)이 오탐보다 훨씬 치명적이다.
   애매하면 경고 쪽으로. "안전합니다" 단언 대신 "확인이 필요합니다".
4. **현재 안드로이드 우선 시연, iOS 확장 가능성 유지** (Flutter 크로스플랫폼).
   지금 iOS에 개발 시간을 쓰라는 뜻이 아니라, **안드로이드에만 있는 기능을 무심코 써서
   iOS를 막는 코드를 피하라**는 의미다 — 표준 위젯·패턴 우선, 플랫폼 종속 부분은 격리.
   웹 호환에는 개발 시간을 쓰지 않는다.
5. **국내 AI 모델만** 사용 (대회 규정) — Upstage Information Extract + Solar Pro.
6. 문서·주석·커밋 메시지·응답은 **한국어**.

## 4. 개발 방법론 (2026-07-03 재정비)

- **깡통 프로토타입 먼저**: 로그인부터 리포트까지 전 화면·네비게이션이 **더미 데이터로
  처음부터 끝까지 관통**하는 앱을 먼저 완성하고, 이후 **화면 단위로 실기능 교체**한다.
- **더미↔실제 교체 규칙**: 화면(위젯)은 **Repository 인터페이스에만 의존**한다.
  `DummyRepository` ↔ `ApiRepository` 구현 교체로 전환하며, 더미 데이터 구조는
  `docs/data-contract.md`의 API 계약과 **항상 동일한 형태**를 유지한다.
  (쉽게 말하면: 화면은 "메뉴판"에만 주문할 뿐 음식이 어디서 나오는지 몰라도 되고,
  주방에서 더미 음식을 내줄지 실제 음식을 내줄지만 나중에 바꿔 끼우면 된다.)
- **승인 게이트**: 각 Phase 종료 시 산출물 요약 + 다음 계획을 보고하고 **사용자 승인 후**
  진행한다. 모호한 기획·의사결정은 임의로 정하지 말고 사용자에게 질문한다.
  확정된 결정은 `/add-decision`으로 decisions.md에 기록한다.
- **서브에이전트 리뷰**: 산출물 단위로 `.claude/agents/`의 리뷰어를 투입한다.
  (gap-checker, persona-jisoo, persona-newlywed, design-reviewer, judge-reviewer,
  rule-auditor — 투입 시점은 docs/plan.md 참고)
- **팀 피드백 분류 원칙**: 팀에서 오는 피드백은 두 종류로 나눠 처리한다.
  ⑴ **지금 프로토타입에서 바꿀 수 있는 것**(색·문구·배치 등) → 반영 여부를 사용자에게
  확인받고 반영. ⑵ **실제 구현을 가정한 것**(당장은 아니지만 미래에 대비할 것) →
  즉시 반영하지 말고 `docs/decisions.md`(또는 CLAUDE.md 7번 백로그)에 "향후 대비"
  항목으로 **기록하고 대비 구조만 열어둔다**. 거친·실구현 관점 피드백을 지금 다 반영하는
  게 아니라, 분류·기록해 재작업 없이 흡수할 수 있게 하는 것이 목적이다.
- **가드레일 방향 (향후 파이프라인)**: `규칙 엔진이 판정 → LLM은 판정 JSON을 받아 설명만
  생성 → 검증` 구조를 전제로 한다. 지금 단계 아키텍처가 이 방향과 충돌하지 않게 유지한다.
- **임시 산출물 정리 규율**: 더미·스캐폴딩·테스트용 임시 파일(더미 리포지토리, 개발용
  스크립트·챗봇, 파이프 검증 엔드포인트 등)은 **실구현으로 대체되면 더 이상 필요 없어지므로,
  깔끔한 폴더 구조·엔드포인트를 위해 때가 되면 삭제/교체**한다. 지금 당장 지우는 게 아니라,
  임시 파일을 만들 때마다 `docs/cleanup-tracker.md`에 "무엇을 / 언제(어느 Phase) 삭제·교체"를
  기록해 두고, 해당 Phase에 도달하면 그 목록을 근거로 정리를 제안·수행한다.

## 5. 문서 맵

| 문서 | 역할 | 상태 |
|---|---|---|
| docs/plan.md | Phase 로드맵·진행 현황 (living) | Phase A 생성 |
| docs/personas.md | 페르소나 2인 정의 (에이전트와 동기화) | Phase A 생성 |
| docs/evaluation-criteria.md | 대회 공식 배점표 (judge-reviewer 근거) | Phase A 생성 |
| docs/IA.md | 정보구조: 화면 트리·네비게이션 맵 | Phase B 예정 |
| docs/user-scenario.md | 화면→버튼→액션 단위 시나리오 | Phase B 예정 |
| docs/api-contract.md | 앱↔서버 JSON 계약 — 더미↔실제 교체 기준점 | Phase D-1 생성 |
| docs/cleanup-tracker.md | 임시·더미·스캐폴딩 정리(삭제·교체) 대상 목록 (living) | Phase D-2 생성 |
| docs/decisions.md | 아키텍처 결정 로그 (**append-only**) | 유지 |
| docs/registry-schema.md | 등기부 추출 스키마 설명 | 유지 |
| docs/draft/ | 브레인스토밍 초안 (**확정 사양 아님**, 참고용) | 유지 |

## 6. 스택·자산

- **Flutter(안드로이드 우선, iOS 확장 여지 유지) + FastAPI**
- 정보 추출: **Upstage Information Extract** — 스키마 `backend/app/schemas/registry_schema.py`
  (최상위 평면 구조, 모든 등기 항목에 `is_canceled`), 멀티 이미지는 PDF 병합 후 1회 호출
- 리포트 문장 생성(향후): **Upstage Solar Pro**
- 실기기 연결: `adb reverse tcp:8000 tcp:8000` → 앱 baseUrl은 `http://127.0.0.1:8000` 고정
  (Wi-Fi 네트워크 무관, USB만 연결되면 동작)
- API 키: `backend/.env`의 `UPSTAGE_API_KEY` (개인 키 → 대회 공식 키 도착 시 값만 교체)
- 검증 완료 자산: `/api/upload` 멀티 이미지 E2E(실기기 확인), JPEG 변환 업로드

## 7. 백로그 (구현 금지, 기록만)

- CODEF 주소 자동조회 (주소 입력 → 등기부 자동 발급)
- 법제처 API 기반 판례 RAG (현재는 큐레이션 판례 라이브러리로 대체)
- 국토부 실거래가 API (전세가율 계산용 시세)
- 안심전세포털 악성임대인 명단 **자동** 대조 (수동 큐레이션 데이터로 우선 대체 가능)
- 계약서 교차검증 (**보류 확정** — 2026-07-03, decisions.md 참고)
- 실시간 등기 변동 알림

## 8. 작업 규칙

- 위험 점수 로직을 다룰 때는 `.claude/rules/risk-scoring.md`를 따른다.
- 백엔드를 다룰 때는 `.claude/rules/api-design.md`를 따른다.
- 새로운 아키텍처 결정을 내리면 `/add-decision` 명령으로 `docs/decisions.md`에 기록한다.
