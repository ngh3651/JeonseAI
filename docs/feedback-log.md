# 팀 피드백 처리 로그

> 🔒 **등기부 실데이터는 마스킹돼 있다** (2026-08-02) — 주민등록번호 `○○○○○○-○******`.
> 숫자·건수·날짜는 검증 근거라 원본 그대로다.

> 팀원·외부(디자인 전공) 피드백이 **어떻게 처리됐는지의 증거**를 남기는 문서.
> 분류 4단계: **채택**(그대로 반영) / **변형 채택**(취지는 수용하되 방식 수정) /
> **반려**(반영 안 함 — 이유 명시) / **보류**(기록만, 향후 대비).
> 반영 원칙: 디자인 토큰 변경은 design_system 토큰 단위로만(화면 하드코딩 금지),
> 색 변경은 WCAG AA(텍스트 4.5:1, 그래픽 3:1) 확인 후, 판정 로직에 닿는 항목은 구현 금지·기록만.

---

## [2026-07-09] 1차 디자인·UX 피드백 라운드 (Phase C~D 산출물 + E 실기능 화면)

- **원본**: `docs/feedback/feedback1~12` (피그마 목업·화면 캡처·디자인 전공 피드백 PDF) + 팀 텍스트 정리
- **상태**: **승인됨(2026-07-09) → steps 1~4 즉시 구현 배치 완료.** 검증: `flutter analyze` 0 이슈 · `flutter test` 2/2 · 백엔드 `pytest` 61/61. 페르소나 재검증·design-reviewer·F9/F11/C2 최종 확정·decisions.md 최종화는 게이트 이후 별도 지시 대기.
- **표기 규칙**: 아래 표의 각 항목 끝 대괄호 `[구현됨 …]`는 실제로 바꾼 파일 위치. 반려·보류는 "코드 무변경".
- **제외**: 마스코트 관련 피드백은 이번 라운드에서 다루지 않음(보류 확정)
- 페르소나 재검증(persona-jisoo·persona-newlywed)은 승인 후 2단계에서 "필요" 표시 항목을 묶어 1회씩 실행

### A. 판례 매칭 화면 (더미 단계 — E-3 실구현 전제)

| # | 피드백 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| A1 | 진입 버튼 "판례 보기"→"판례 매칭" | 채택 | 화면 제목(판례 매칭)과 라벨 통일 — 인지 부하 감소. 단 [2026-07-04] "진입 라벨=판례 보기" 결정과 상충 → 기존 항목에 재검토 주석 + 새 결정 기록 | 불요 | XS | 즉시 |
| A2 | "판례" 용어 툴팁 제거 | 변형 채택 | 툴팁 어색함의 실체는 A3 렌더링 버그(공백)임. 첫 등장 1회 툴팁은 [2026-07-04] 사용자 확정 결정이라 유지, A3 수리로 "일반 문장처럼" 보이게 해소. 완전 제거는 반려 | 필요(지수: 수리 후에도 툴팁 필요한지) | A3에 포함 | 즉시 |
| A3 | 툴팁 단어 앞뒤 공백 과다 (전 화면 공통) | 채택 | 원인 확정: `termSpan()`이 `inline: true` 미전달 → 48dp 최소 박스+Center 렌더 (term_tooltip_sheet.dart:88). 버그 수리 성격 | 불요 | S | 즉시 |
| A4 | 카드 내 "결과"↔"공통점" 순서 교체 + 결과 빨강 더 밝게 | 채택(순서) + 조건부 채택(색) | "왜 나한테(공통점)→어떻게 됐나(결과)" 인과 순서가 자연스러움. 밝은 빨강은 AA 통과 후보 존재(#D93025 4.77:1, #D32F2F 4.98:1 — #E53935는 4.23:1로 실패) → 토큰 danger 계열에서 일괄 조정(F11과 세트) | 불요 | S | 즉시(더미) → E-3 유지 |
| A5 | "이런 피해를 피하려면"을 카드별 맞춤 조언으로 | 변형 채택 | 앱의 궁극 목적(회피 행동 유도)과 직결 — E-3 판례 큐레이션 데이터 양식에 `advice`(카드별 조언) 필드 추가, 정민재가 판례별로 기입(비개발 기여 경로 유지). 하단 공통 콜아웃은 질문 생성기 CTA로 축소 유지 | 필요(조언 문구 톤) | M | **E-3 묶음** |
| A6 | 유사율 % + 유사순 정렬 + 위험도 병기 | 변형 채택 | 원시 %(코사인 유사도)는 반려 — 제안자 스스로 보완했고, 우리 매칭은 임베딩이 아닌 규칙 기반이라 %가 성립하지도 않음. 대신 **매칭된 위험 패턴 뱃지 표시 + 매칭 개수 기반 정렬 + 판례 결과 심각도 병기**로 설계(E-3 설계 입력). 정렬 기준은 decisions.md에 기록(판정 아님·표시 순서지만 투명성 유지) | 필요(이해도) | M | **E-3 묶음** |

### B. 계약 여정 화면

| # | 피드백 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| B1 | 계약여정 전문용어에 툴팁 추가 | 채택 | 전입신고·확정일자·보증보험 등 여정 화면이 오히려 용어 밀도 높음. 기존 TermText 재사용 | 필요(지수: 용어 선정) | M | 즉시 |
| B2 | "단계" 어색한 줄바꿈 | 채택 | content_repository.dart:216 subtitle 문구를 줄바꿈 안전하게 재작성(예: "…보증금을 지키는 가장 중요한 일") — 폭 제어보다 문구 수정이 견고 | 불요 | XS | 즉시 |
| B3 | "등기부"→"등기부등본" 통일 | 변형 채택 | 신뢰감 취지 수용. 사용자 노출 문구 약 15곳 통일. 단 "등기사항전부증명서"는 발급 가이드 화면에서만 괄호 병기(본문 강제 시 가독성 저하). 코드 내부 명칭은 제외 | 불요 | S | 즉시 |

### C. 손실 시뮬레이터

| # | 피드백 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| C1 | 슬라이더에 평균 낙찰가율 마커(▲ 평균 78%) + 참고 문구 | 변형 채택(조건부) | UI(마커+참고 문구)는 목업대로 수용. **단 "78%"는 현재 출처 미확보 — decisions.md에 낙찰가율 출처 항목이 아직 없음**(E-4가 "낙찰가율 출처 필수"로 계획됨). 출처 없는 수치 금지 원칙상 수치 표시는 E-4 출처 기록 후 | 불요 | M | **E-4 묶음** |
| C2 | "돌려받을 것으로 보는 금액"→"예상 반환금" + 결과 카드에 손실액과 병기 강조 | 채택 | 문구 간결화 + 결과 카드에서 손실액·반환금 함께 표시(사용자 진짜 궁금증). "반환"이 지수에게 어려울 수 있어 페르소나 검증 대상 | 필요 | S | 즉시 |
| C3 | 보증보험 토글 제목·안내·버튼 문구 3종 교체 | 채택 | 목업(feedback4) 그대로: "보증보험 적용 시 시뮬레이션" / 안내 간결화 / "보증보험 가입 가능 여부 확인하기". 버튼 동작(리포트 보험 카드 복귀)과 문구 의미 일치 확인됨 | 필요(서연: 빠른 이해) | S | 즉시 |

### D. 리포트 화면

| # | 피드백 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| D1 | '지금 해야 할 일' 강조 + 근거별 해결 방안 매칭 | 변형 채택 | ⑴ 제목이 현재 12px label — 시각 강조(크기·위계 상향)는 즉시 채택. ⑵ 근거별 해결 방안은 nextAction 결정적 템플릿 원칙([2026-07-07])과 정합 필요 — 서버 템플릿 확장이라 A5(판례별 조언)와 함께 E-3에서 설계 검토 | 필요 | S(강조) / M(매칭) | 즉시 / E-3 검토 |
| D2 | 위험도 등급 세분화(5~7단계) | **반려(구현) + 기록** | 판정 체계 변경이라 이번 라운드 구현 금지(지시 확인). 영향 분석: ⑴ 5~7단계 각 경계 임계값 전부에 권위 출처가 필요해지는데 현존 출처(HUG 가입요건, 안심전세포털)는 3단계 구도 ⑵ worst-of 결합([2026-07-06]) 전면 재설계 필요 ⑶ 중간 등급이 "덜 위험"으로 오독되면 보수적 편향 위반 위험. [2026-07-06] "3단계→세분화 가능한 확장 구조 유지"가 이미 대비 중 → 해당 항목에 재검토 요청 주석 추가 | 불요 | — | 기록만 |
| D3 | 근거 카드 용어 공백 | 채택 | A3과 동일 수리로 해소(같은 termSpan 경로) | 불요 | A3 포함 | 즉시 |
| D4 | 면책 안내 문구 추가 | 채택 | 현재 전역 디스클레이머 부재 확인. 팀 제공 원문 사용. **노출 위치 제안: 리포트 최하단(하단 CTA 아래) 상시 작은 회색 텍스트** — 캡처 공유 시 자연 포함. 판례·시뮬레이터는 기존 "예시·참고" 고지가 있어 중복 배치 안 함. 온보딩 삽입은 반려(읽지 않고 지나침) | 필요(서연: 신뢰 균형) | S | 즉시 |

### E. 홈 매물 카드 (질문 성격 — 답변+기록)

| # | 피드백 | 분류 | 답변·근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| E1 | 보증금 직접 입력? "보증금"="전세가"? → "전세보증금" 라벨 | 변형 채택 | **답: 보증금은 사용자 직접 입력이 맞음**(등기부에 없음 — [2026-07-03] 결정). 입력 화면 라벨을 "예정 전세보증금 (필수)"로 명확화. 홈 카드·리포트의 "보증금"은 공간상 유지(첫 입력 지점에서 개념이 잡히면 축약 무해) | 필요(라벨 확인) | XS | 즉시 |
| E2 | 월세·반전세 지원 범위? | 보류(기록) | **답: MVP는 전세 전용.** 월세·반전세는 전월세 전환율 등 별도 기준(출처) 필요 → decisions.md에 스코프 명시 + 백로그 추가 | 불요 | — | 기록만 |
| E3 | 카드 3번째 줄 형식 비일관 | 변형 채택 | 실질은 [2026-07-08] topRiskSummary 신호 우선순위 수리로 대부분 해소(위험 신호가 시세 안내에 안 가려짐 — 테스트 봉인). 잔여: 안내문("시세를 입력하면…")의 톤을 수치 요약과 통일하는 소폭 문구 조정만 검토(서버 문구) | 불요 | XS | 즉시(서버 문구) |
| E4 | 주소가 건물 단위 표기라 어색 | 보류(기록) | **답: 실데이터로 자연 해소되는 부분이 큼** — 추출 스키마 `address`는 표제부 소재지 전체를 받아 집합건물이면 동·호수 포함. 홈 카드는 alias(기본값=주소) 표시. 다가구·단독은 건물 단위가 등기 자체의 한계 → E5와 함께 기록 | 불요 | — | 기록만 |
| E5 | 다가구주택 처리? | 보류(기록) | **답: 구조적 한계 명시가 정답** — 등기(건물 단위)와 계약(세대 단위) 불일치, 다른 세입자 선순위 보증금은 등기부에 없음([2026-07-06] E-1 기준① 참고에 부분 기록). 리포트 한계 고지 문구는 insurance 한계 고지 패턴 재사용 후보로 기록 | 불요 | — | 기록만 |
| E6 | 시세 최신화 방침? | 보류(기록) | **답: 시세는 분석 시점 입력값 스냅샷.** 최신화는 재분석(stale 배너, 7일)이 현행 대응이고, 자동 최신화는 백로그 "국토부 실거래가 API"와 연결해 기록 | 불요 | — | 기록만 |

### F. 전역 디자인 (디자인 전공 피드백 — 목업 feedback8~12)

| # | 피드백 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| F1 | 배경 F5F5F5 | 채택 | 현재 #F5F8F6(연초록빛) → #F5F5F5(중립 회색). 명도 거의 동일, 초록 틴트만 제거 — 흰 카드 대비 목적(F6과 세트). 토큰 1줄 | 불요 | XS | 즉시 |
| F2 | 버튼 라운딩 14→8, 미니 7 | 변형 채택 | 실측 현재값은 16(AppRadius.lg — 피그마 문서의 14와 다름). 취지(신뢰감) 수용해 **버튼만** 8/7 신규 토큰으로, 카드 radius 16은 유지(피드백이 버튼 한정) | 불요 | S | 즉시 |
| F3 | 라인 버튼 흰 배경 지양 | 변형 채택 | 전면 금지 대신 규칙화: **흰 카드 위 outline → tonal(연초록 filled)** 교체, 회색 배경 위는 목업 수정안 따름("등기부등본이 없어요"도 tonal). AppTonalButton 신설(F8과 세트) | 불요 | M | 즉시 |
| F4 | 게이지 '확인 필요' 줄바꿈 | 채택 | safety_gauge 중앙 라벨 2줄 처리(목업 feedback10). 덤: RiskBadge에 오버플로 처리 부재 확인(긴 decisionLabel) — 함께 수리 | 불요 | S | 즉시 |
| F5 | 카드 패딩 가로 18/세로 16 | 채택 | 현재 all(16) → symmetric(18/16) 토큰화 | 불요 | XS | 즉시 |
| F6 | 카드 테두리 제거 | 채택 | 배경 대비(F1)로 구분 성립. cardTheme side 제거. line 토큰 자체는 유지(챗봇 말풍선 등 다른 용도) | 불요 | XS | 즉시 |
| F7 | 위험도 뱃지 위치 변경 | 변형 채택 | 문제의 실체는 근거 카드(질문형 긴 제목)라 **EvidenceCard만 뱃지를 제목 위로**(목업 feedback11). 홈 카드는 제목이 짧고 chevron 구도라 유지, 오버플로 방어만 추가 | 불요 | S~M | 즉시 |
| F8 | '시세 입력하기' 연초록 filled | 채택 | F3 tonal 버튼의 대표 적용례(목업 full-width) | 불요 | F3 포함 | 즉시 |
| F9 | '판정·규칙 기반' 칩 회색으로 | 조건부 반려(현행 유지 추천) | **목업 '수정 제안 2'(판정 칩 연초록+AI 칩 회색)는 현행과 동일** — 실제 쟁점은 "둘 다 회색(제안 1)"뿐. 이 칩은 가드레일 가시화 장치라 위계 유지 추천. 페르소나 2인에게 "회색이어도 '판정은 규칙' 메시지가 전달되는가"를 검증 후 확정. 같은 컴포넌트의 이월 안건("설명·AI 생성" 라벨 정확성 — plan.md E-2 이월)도 이때 함께 결정 제안 | **필요(결정 조건)** | XS | 페르소나 후 |
| F10 | 챗봇 말풍선 꼬리 정리 + 프로필 마지막만 | 채택 | 라운딩 통일(모서리 비대칭 제거)이 목업 수정안과 부합. 연속 메시지 그룹핑(마지막에만 프로필)은 현재 미구현 → 추가 | 불요 | S~M | 즉시 |
| F11 | 게이지 색 맑고 밝게, 확인필요는 노랑 | 변형 채택(AA 조건부) | 계산 결과: 순수 밝은 노랑(#F9A825 1.97:1)·밝은 초록(#43A047 3.30:1)은 텍스트 AA 실패. **절충: 텍스트/뱃지 글자는 AA 통과 범위에서 최대한 밝게**(danger #D32F2F 4.98 / caution 앰버 #A16207 4.92 / ok #0E7A3D 5.43), **게이지 호(그래픽 3:1)는 한 단계 더 밝게** 분리. "글자 얇은 테두리"는 반려(가독성 저하·비표준) — 대비 통과 색으로 해결 | 불요(시각) | S~M | 즉시 |
| F12 | 분석·재분석 버튼 ○ 아이콘 제거 | 채택 | 실체는 Icons.search(분석 CTA)·Icons.refresh(재분석). 해당 두 버튼만 제거, 에러 화면 "다시 시도"의 refresh는 유지 | 불요 | XS | 즉시 |

### G. 신규 기능 제안 (G3만 예외 검토, 나머지 백로그)

| # | 피드백 | 분류 | 근거 | 시점 |
|---|---|---|---|---|
| G1 | 다크모드 | 보류(백로그) | 토큰 구조가 이미 분리돼 있어 향후 대비됨. 지금은 시연 범위 밖 | 기록만 |
| G2 | FAQ | 보류(백로그) | 용어 챗봇(E-5)·발급 가이드와 역할 중복 가능성 함께 기록 | 기록만 |
| G3 | 로딩 스피너+순환 멘트 | 변형 채택(소규모) | 실분석 대기가 1~2분으로 길어 체감 효과 실재 + 작업량 작음. 기존 4단계 유지, **가장 긴 마지막 단계(물결 구간)에 순환 보조 멘트 2~3개 추가**("세이프가 집을 지키는 중입니다" 등 — 텍스트라 마스코트 보류와 무관). 스피너 교체는 반려(마스코트+진행 바 구도 유지). plan.md E-2 이월 "로딩 고정 연출 Phase F 재검토"와 연결 | 즉시(소규모) |
| G4 | 스크롤 글자 애니메이션 | 보류(백로그) | 폴리시 성격 — Phase F(시연 영상) 때 가치 재평가. 지금은 회귀 리스크 대비 효용 낮음 | 기록만 |
| G5 | 실시간 검색어·인기 매물 | 반려(백로그 기록) | 서버·다중 사용자 데이터 전제 — 현행 "기기 로컬 단일 저장"([2026-07-03]) 구조와 불일치. 조회 데이터 수집은 프라이버시 고지 필요. 시연 범위 밖 | 기록만 |
| G6 | 결제 시스템 | 반려(백로그 기록) | 시연 불필요(지시 확인). 수익모델 아이디어로만 기록 | 기록만 |
| G7 | 중요 문구 볼드 + "부제→상세" 구조 | 변형 채택 | 카드 제목이 이미 질문형 부제 구조라 방향 일치. LLM 생성 문장은 마크업 불가(가드레일상 플레인 텍스트) → **결정적 영역(금액 박스·nextAction·결과 카드)만** 볼드 강조 검토. D1과 묶어 처리 | 즉시(D1과) |

### H. 팀 질문 답변 (구현 아님)

- **H1. 기술 한계로 못 만든 기능 — 버튼을 뺄까, '추후 구현'으로 남길까?**
  제안 방침(이원화):
  1. **시연 대본 동선에 없는 미구현 기능 = 기능 플래그(상수)로 숨김** + 레이아웃은 남은 요소로 재배치(그리드 홀수 칸은 카드 span 조정으로 균형). 눌렀을 때 아무 일도 안 일어나는 버튼이 최악 — 영상·실기기 시연 모두에서 미완성 인상.
  2. **로드맵 어필 가치가 있는 기능(예: 주소 자동조회)만 '준비 중' 배지+비활성으로 남김** — 성장성(20점) 어필 신호로 역이용. 단 화면당 1개 이내.
  3. 판단 기준: "심사위원이 그 버튼을 눌러보면 어떻게 되는가"로 결정. 이미 스코프 결정([2026-07-03])에서 주소 검색은 "UI만 두고 더미"로 확정된 전례와 일관.
- **H2. 실사례(사기 케이스)가 드문데 테스트는?** — 팀 공유용 한 줄:
  > "위험 케이스는 수제 픽스처(등기 JSON)로 무한 제조해 규칙 엔진을 검증하고, 실물 검증은 법원경매정보의 경매 물건 등기부(위험 등기가 실제 붙은 문서)를 발급받아 사용합니다 — 방법은 docs/registry-sample-guide.md."

---

## 구현 결과 (2026-07-09 steps 1~4 — 실제로 바꾼 것만)

> 표의 분류와 매핑. 파일 경로는 `frontend/lib/` 또는 `backend/app/` 기준.

**step 1 — 디자인 토큰**
- F1 배경 `#F5F8F6→#F5F5F5` — [구현됨 design_system/tokens/app_colors.dart `background`]
- F2 버튼 라운드 8/미니 7 신규 토큰(카드 16 유지) — [구현됨 tokens/app_spacing.dart `AppRadius.button/buttonMini` + theme.dart filled/outlined]
- F5 카드 패딩 symmetric(18,16) — [구현됨 tokens/app_spacing.dart `cardPadH/cardPadV` + components/app_card.dart]
- F6 카드 테두리 제거 — [구현됨 theme.dart cardTheme]
- F11+A4 색 — [구현됨 app_colors.dart danger/caution/ok + dangerArc/cautionArc/okArc + `gradeArcColor()`], 대비비 주석 병기

**step 2 — 공통 컴포넌트**
- A3·D3 툴팁 공백 버그 — [구현됨 components/term_tooltip_sheet.dart `TermText`(inline 분기·48dp 박스 제거) + 호출부 loss_simulator]
- F3·F8 톤 버튼 — [구현됨 components/app_button.dart `AppTonalButton` 신설·`AppCompactButton.tonal`; 적용 report_screen(근거 액션)·loss_simulator(시세 입력하기)]
- F4 게이지 2줄 + 뱃지 오버플로 — [구현됨 components/safety_gauge.dart(ConstrainedBox 2줄, arc 색 분리) + risk_badge.dart(softWrap/maxLines)]
- F7 근거 카드 뱃지 제목 위 — [구현됨 components/app_card.dart `EvidenceCard`]
- F10 챗봇 말풍선 — [구현됨 components/chat_bubble.dart(라운딩 통일·showAvatar) + screens/chatbot/glossary_chatbot_screen.dart(연속 판정)]
- F12 아이콘 제거 — [구현됨 screens/home/home_screen.dart(`매물 분석 시작하기` search 제거); 에러 refresh 유지]

**step 3 — 공통 문구**
- B3 등기부→등기부등본(~15곳) — [구현됨 content_repository·analysis_repository·home·onboarding(2)·guide(+등기사항전부증명서 병기)·report·property_search(3)·loading]
- B2 여정 '단계' 문구 — [구현됨 content_repository.dart `입주하고 나서 꼭 할 일` subtitle]
- C2 예상 반환금 + 결과 병기 — [구현됨 screens/simulator/loss_simulator_screen.dart `_lossCard`·`_detailRow`]
- C3 보증보험 토글 3문구 — [구현됨 loss_simulator_screen.dart(제목·subtitle·확인 버튼)]
- E1 예정 전세보증금 라벨 — [구현됨 screens/search/property_search_screen.dart(라벨·안내문)]
- A1 판례 매칭 라벨 — [구현됨 screens/report/report_screen.dart `_actionArea`]
- D4 면책 최하단 — [구현됨 report_screen.dart `_disclaimer`(textMuted, AA 5.04:1, caption)]
- E3 홈 카드 시세 안내 톤 — [구현됨 backend fallback_texts.py + dummy_data.py + frontend analysis_repository.dart + test_rule_engine.py 봉인 갱신]

**step 4 — 화면별**
- D1 '지금 해야 할 일' 강조 — [구현됨 report_screen.dart `_nextActionCard`(label 12px→title 17px)]
- G7 볼드 — [구현됨 report_screen.dart nextAction w700 + loss_simulator `_lossCard` 손실액·반환금 w700]
- G3 로딩 순환 멘트 — [구현됨 screens/loading/analysis_loading_screen.dart `_waitingMents` 마지막 단계 순환]

**반려·보류 (코드 무변경)**: D2(등급 세분화·decisions 주석만) · F9(판정 칩 현행 유지·페르소나 대기) · A6 원시%(규칙 매칭 부적합) · F11 글자 테두리(대비 색으로 해결) · E2·E4·E5·E6·G1·G2·G4·G5·G6.
**E-3로 이관(설계 메모)**: A4 순서(카드 구조 재작업과 함께) · A5 advice(decisions.md 설계 메모 기록) · A6 뱃지·정렬 · D1b 근거별 해결 매칭.
**E-4로 이관**: C1 평균 낙찰가율 마커(출처 확보 후).
**후속 후보**: 백엔드 `questions.py`(중개사 질문)·`extraction.py`(에러 메시지)의 "등기부"는 이번 스윕(설명·폴백 텍스트) 범위 밖 — 다음 문구 패스에서 통일 검토(백엔드 테스트 동반).

### 게이트 이후 2차 (2026-07-09 — 페르소나·PM 락·백엔드 스윕)
- **F9(판정 칩)**: persona-jisoo·persona-newlywed 모두 "회색화하면 신뢰 신호 약화 → 초록 유지" → **현행 유지(코드 무변경) 확정**. [구현 없음]
- **F11 게이지 색**: **앰버 유지 락(PM)** — '확인 필요'가 '조금 위험'으로 읽혀도 보수적 편향에 부합. 색 단독 신호 아님(게이지 중앙 '확인 필요' 텍스트 상시 노출) 확인. [구현 없음]
- **C2 '예상 반환금'**: **유지 락(PM)** — 통용 어휘. [step 3에서 이미 반영]
- **E-2 라벨 '설명·AI 생성' → '설명·자동 생성'**: 페르소나 2인 모두 "폴백에도 AI라 표기는 부정확 → 자동 생성이 정직" → 반영 — [구현됨 components/source_label.dart]
- **B3 백엔드 스윕**: `fallback_texts.py`(8곳)·`explanation.py`(프롬프트 예시 1곳) "등기부"→"등기부등본" — [구현됨], `pytest` 61/61.

## 기존 결정과의 상충 (append-only 주석 대상 — 처리 완료)

| 피드백 | 상충 결정 | 처리 |
|---|---|---|
| A1·A2 (판례 라벨·툴팁) | [2026-07-04] 판례 용어 유지(진입 라벨 "판례 보기" + 첫 등장 툴팁) | 기존 항목에 "재검토 요청 있었음(2026-07-09, 팀 디자인 피드백)" 주석. A1은 새 결정 항목으로 라벨 변경 기록, A2는 툴팁 유지 이유 명시 |
| D2 (등급 세분화) | [2026-07-06] E-1 향후 과제 "3단계 → 세분화 가능한 확장 구조 유지" | 기존 항목에 "재검토 요청 있었음(2026-07-09) — 3단계 유지 이유: 등급별 임계값 출처 부재 + 정부 안심전세 3단계 표준 + 보수적 편향" 주석 |

## 미뤄둔 피드백 목록 대조 (decisions.md 유보 항목 + plan.md E-2 이월)

| 기존 유보 항목 | 이번 라운드 상태 |
|---|---|
| [E-2 이월] "설명·AI 생성" 라벨 정확성 (사용자 결정 대기) | **이번에 함께 처리 제안** — F9와 같은 컴포넌트(VerdictSourceLabel)라 페르소나 검증 때 묶어 결정 |
| [E-1 향후] 낙찰가율 지역·유형 세분화 | 여전히 보류 — C1이 E-4로 연결되며 "평균 참고값 표시" 요구가 추가됨(출처 확보 필요성 강화) |
| [E-1 향후] 위험 등급 3단계→세분화 확장 구조 | 여전히 보류 — D2로 재검토 요청 접수, 현 상태 유지(주석 추가) |
| [E-2 이월] 로딩 "실시간 표시" 고정 연출 | 여전히 보류(Phase F) — G3 반영 시 같은 화면을 만지므로 위치만 연결 기록 |
| [C-3 유보] 매물 나란히 비교 뷰 | 여전히 보류(백로그) — E3(카드 요약 변별력)은 [2026-07-08]에서 별도 해소됨 |
| [C-3 유보] S-04 입력 초안 복원 / 이력 손상 처리 / 세션·온보딩 영속화 | 여전히 보류 — 이번 피드백과 무관 |
| [C-3 유보] 상담처 실제 링크·전화 | 여전히 보류(Phase F) |
| [E-2 이월] 질문 우선순위 플래그 / 유령 이력 / 타임아웃 이중 적용 | 여전히 보류(각 E-3 이후 / F 전 / E-6) |
| (신규 추가) | 월세·반전세 지원(E2), 다가구 한계 명시(E5), 시세 최신화(E6), 다크모드(G1), FAQ(G2), 스크롤 애니메이션(G4), 인기 매물(G5), 결제(G6), 근거별 해결 방안 매칭(D1b→E-3 검토) |

## 2단계 구현 순서 제안 (승인 후)

1. **디자인 토큰**: F1 배경 → F2 라운딩 → F5 패딩 → F6 테두리 → F11+A4 색(AA 재검증 포함)
2. **공통 컴포넌트**: A3/D3 툴팁 인라인 수리 → F3+F8 tonal 버튼 → F4 게이지·뱃지 줄바꿈 → F7 뱃지 위치 → F10 챗봇 말풍선 → F12 아이콘
3. **공통 문구**: B3 등기부등본 통일 → B2 → C2 → C3 → E1 → A1 → D4 면책 → E3(서버 문구)
4. **화면별**: D1 제목 강조(+G7 볼드) → G3 로딩 멘트
5. **E-3/E-4 설계 입력 전달**: A4(유지)·A5·A6·D1b → E-3 / C1 → E-4
6. **검증·기록**: persona-jisoo·persona-newlywed 재검증(문구 항목 일괄 + F9 결정) → design-reviewer → `flutter test` 회귀 → decisions.md 주석·신규 항목 → CLAUDE.md 백로그 갱신 → cleanup-tracker 확인

---

## [2026-07-27] OCR 하이라이트 야간 작업에서 나온 발견 (**기능 채택 미확정**)

- **원본**: `docs/ocr-highlight-findings.md` (검증 기록) · `docs/night-log.md` (진행 로그)
- **상태**: **기능 자체가 채택 미확정.** 아래 두 항목은 **기능을 폐기하더라도 유효한
  발견**이라 여기 남긴다. `decisions.md`에는 반영하지 않았다(승인 대기).
- **주의**: 아래 항목은 전부 **판정 계층에 닿는다**. 반영하려면 권위 출처 확인 + 별도 승인이 필요하다.

### Z. 등기부 실데이터에서 나온 구조적 발견

| # | 발견 | 분류 | 근거 | 페르소나 | 작업량 | 시점 |
|---|---|---|---|---|---|---|
| Z1 | **공동명의 다수 권리자 — 리포트에서 소유자를 1명으로 가정하면 안 된다** | 보류(기록) | 검증에 쓴 등기부는 소유권이 3번 이전돼 현재 **공유자 여러 명**이다(갑구에 이름 5명 등장, 그중 옛 소유자 포함). 지금 코드베이스에서 "공동명의"라는 말이 나오는 곳은 하이라이트 시트 caution **한 군데뿐**이고, 그마저 세션 한정 화면이라 앱을 껐다 켜면 사라진다. 계약 실무에서 사고가 잦은 항목이 표시 계층에만 있는 것은 위험하다. **이 기능을 폐기해도 유효한 발견.** ⚠ 근거 카드·판정으로 승격하려면 규칙 엔진 변경이라 별도 승인 필요 | 필요(서연: 배우자 공유 시 필요 정보) | M | **별도 승인 후** |
| Z2 | **개인/법인이 등록번호 형태로 구분 가능** | 보류(기록) | 개인 주민번호는 뒤 7자리가 마스킹(`○○○○○○-○******`), 법인등록번호는 숫자(`110111-0015671`). 실데이터로 확인. 근저당권자가 은행(법인)인지 개인인지 구분하면 위험 문구를 갈라 쓸 수 있다(개인 채권자는 다른 성격). ⚠ 판정에 쓰려면 출처·기준 필요 | 불요 | S | 기록만 |
| Z3 | 등기부 꼬리말에 `N/M` 쪽수 표식 + 발급확인번호가 있다 | 보류(기록) | 실측: `1/5`, `5/5`, `발급확인번호 ABCD-EFGH-0000`(전 페이지 동일). **쪽 누락·사진 순서 어긋남·다른 등기부 섞임**을 판별할 수 있는 신호다. 지금은 하이라이트 표시 억제에만 쓰지만, **IE 경로에서도** 쓸 수 있는지(예: 페이지 누락 시 사용자에게 재업로드 요청) 검토 가치가 있다. ⚠ 판정·게이트로 승격하려면 별도 승인 | 필요(누락 안내 문구) | M | **별도 승인 후** |

### 라운드 3 페르소나·디자인 리뷰 처리 (하이라이트 화면 한정)

`.claude/agents/`의 persona-jisoo · persona-newlywed · design-reviewer를 실제 호출해
새 화면(S-07b 원본에서 보기)을 리뷰했다. **처리 상세는 `docs/ocr-highlight-findings.md` §5**에
표로 정리했다(고친 것 14건 / 기록만 7건 / 반려 0건). 이 화면은 채택 미확정 기능이라
여기서는 요지만 남긴다.

- **3인 공통 최다 지적**: "표시가 없는 것"과 "AI가 안 본 것"이 화면에서 구분되지 않아
  **리포트 신뢰까지 깎인다** → `Report.checkedNotes`("무엇을 찾아봤고 무엇을 왜 표시하지
  않았는지")를 신설해 반영. **채택**
- **design-reviewer 실측**: 우리 빨강과 등기부 인쇄 빨강(취소선)의 대비가 **1.24:1**로
  사실상 같은 색인데 **의미는 정반대**(원본 빨강=말소, 우리 빨강=살아있는 위험) →
  흰 halo로 형태 구분. **채택**
- **뱃지 대비 3.16:1 (AA 미달)** → `AppColors.caution`으로 교체해 4.92:1. **채택**
  (토큰 단위 변경 — 화면 하드코딩 색은 전부 `AppColors.marker*`로 승격)
- **소유자 구성을 리포트 근거 카드로 승격**(서연) → **보류**: 판정 계층 변경이라
  이번 작업 금지 사항. 위 Z1로 기록.
