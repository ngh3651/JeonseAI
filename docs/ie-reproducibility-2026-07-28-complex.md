# Information Extract 재현성 영점 측정 (2026-07-28)

> `backend/scripts/measure_ie_reproducibility.py` 가 생성했다.
> **개인정보 없음** — 값 대신 6자리 해시와 개수만 기록한다.

## 왜 쟀나

`out/ie_*.json` 3건(같은 문서)의 배열 길이 합계가 **18 / 16 / 16**으로 갈렸다.
이것이 모델의 흔들림인지 입력 차이인지 모르면, 다른 LLM과 비교했을 때 나온 차이가
**모델 차이인지 노이즈인지 구분할 수 없다.** 모든 AI 비교 실험의 영점이 여기다.

## 측정 조건

- 입력: 같은 이미지 3장 (`page1.png, page2.png, page4.png`)
- 반복: 5회 (성공 5회)
- 모델·엔드포인트·스키마: 운영 경로와 동일 (`app/services/extraction.py`)

## 1. 응답 시간

| 회차 | 초 |
|---|---|
| 1 | 17.4 |
| 2 | 17.1 |
| 3 | 18.5 |
| 4 | 18.8 |
| 5 | 16.7 |
| **중앙값** | **17.4** |
| **최소~최대** | 16.7 ~ 18.8 |

## 2. 배열 길이 — 회차마다 같은가

| 필드 | 1회차 | 2회차 | 3회차 | 4회차 | 5회차 | 판정 |
|---|---|---|---|---|---|---|
| `current_owners` | 1 | 1 | 1 | 1 | 1 | 고정 |
| `ownership_changes` | 1 | 1 | 1 | 1 | 1 | 고정 |
| `provisional_seizures` | 1 | 1 | 1 | 1 | 1 | 고정 |
| `provisional_dispositions` | 0 | 0 | 0 | 0 | 0 | 고정 |
| `seizures` | 1 | 1 | 1 | 1 | 1 | 고정 |
| `auction_commencements` | 1 | 1 | 1 | 1 | 1 | 고정 |
| `trust_registrations` | 0 | 0 | 0 | 0 | 0 | 고정 |
| `mortgages` | 3 | 3 | 3 | 3 | 3 | 고정 |
| `jeonse_rights` | 0 | 0 | 0 | 0 | 0 | 고정 |
| `lease_registrations` | 0 | 0 | 0 | 0 | 0 | 고정 |
| **합계** | 8 | 8 | 8 | 8 | 8 | 고정 |

## 3. 스칼라 필드 — 값이 같은가 (해시 비교, 원문 미출력)

| 필드 | 1회차 | 2회차 | 3회차 | 4회차 | 5회차 | 판정 |
|---|---|---|---|---|---|---|
| `address` | `67503a` | `67503a` | `67503a` | `67503a` | `67503a` | 고정 |
| `document_title` | `c28380` | `c28380` | `c28380` | `c28380` | `c28380` | 고정 |
| `exclusive_area_sqm` | `509993` | `509993` | `509993` | `509993` | `509993` | 고정 |

## 4. 항목 지문 — 같은 항목이 **같은 내용으로** 오는가

원소별 해시를 정렬해 비교한다(순서가 바뀌어도 집합이 같으면 '고정').
**길이가 같아도 내용이 다를 수 있다** — 이 표가 그것을 잡는다.

| 필드 | 판정 | 비고 |
|---|---|---|
| `current_owners` | 고정 |  |
| `ownership_changes` | 고정 |  |
| `provisional_seizures` | 흔들림(2종) | 교집합 0건 / 회차별 개수 [1, 1, 1, 1, 1] |
| `provisional_dispositions` | 고정 |  |
| `seizures` | 고정 |  |
| `auction_commencements` | 고정 |  |
| `trust_registrations` | 고정 |  |
| `mortgages` | 고정 |  |
| `jeonse_rights` | 고정 |  |
| `lease_registrations` | 고정 |  |

### 4-1. 흔들린 배열의 **어느 필드**가 달랐나 (필드 이름만, 값 미출력)

| 배열 | 회차마다 값이 달라진 필드 | 회차마다 같았던 필드 |
|---|---|---|
| `provisional_seizures` | `cause` | `claim_amount`, `claimant`, `is_canceled`, `purpose`, `rank_number`, `receipt_date` |

## 5. 결론

**항목 내용이 흔들린다**(길이는 같아도): `provisional_seizures`

→ **이것이 이 실험의 영점(noise floor)이다.** 앞으로 다른 provider와 비교할 때,
   여기서 흔들린 배열·필드에 나타난 차이는 **모델 차이로 볼 수 없다.**
   비교표를 읽기 전에 반드시 이 절을 먼저 볼 것.

### 이 측정의 한계 — 축소해서 읽지 말 것

- 표본은 **문서 1건 × 5회**다. 다른 등기부에서 같다는 보장이 아니다.
- 온도·seed 같은 호출 파라미터를 우리가 고정한 것이 아니다 — 서버 기본값에 의존한다.
- '고정'은 **이 회차 수 안에서** 관측되지 않았다는 뜻이지, 흔들리지 않는다는 증명이 아니다.

