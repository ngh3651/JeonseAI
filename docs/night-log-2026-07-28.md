# 야간 자율 세션 진행 로그 — 2026-07-28

> 시간순 작업 기록. 아침에 읽을 요약은 `docs/morning-report-2026-07-28.md`.
> 브랜치: `feat/reading-guide` (기점 `5ffcd35` = main = dev)

---

## 00:00 시작 — 브랜치 생성 + 사전 조사

`main`에서 `feat/reading-guide` 분기. 워킹트리 clean.

### findings §9 확인 결과 — **충돌 없음**

지시서가 "채팅 세션은 §9를 보지 못했다"고 경고했으나, 읽어 보니 **충돌이 아니라
후속 연결**이었다:

| §9 항목 | 이번 지시와의 관계 |
|---|---|
| §9.5 `[보류] 표시 종류가 늘면 줄무늬가 될 수 있다` | **작업 A-3이 정확히 이 후속.** "실제로 종류를 추가하는 시점에 화면을 보고 재검토한다"고 적혀 있고, 지금이 그 시점이다 |
| §9.1·§9.6 진입 카드 뱃지 `위험 n곳` | 확정 5가 `따져볼 곳 n곳`으로 교체. §9.6은 "위험 0건이면 `확인할 곳 n곳`(초록)"이라는 분기도 기록해 뒀다 — 그 분기를 유지한 채 문구만 바꾼다 |
| §9.4 색각 대비 미해결 | 작업 D의 "고려했으나 하지 않은 것"에 그대로 이관 |
| §9.9 열람일시 이미 읽고 있음 | A-1 `viewed_at` 마크가 그 좌표를 재활용 — 새 OCR 호출 불필요 |

---

## 00:20 [1] 저장된 OCR로 앵커 문자열 실측 (착수 전 확인, 크레딧 0원)

지시서 A-2가 요구한 "R4 3종이 실제 샘플에 나오는지" 확인. 두 샘플 문서를 다 봤다.

**문서 ①** `out/ocr_page_*.json` — 서초동 5장(실기기 업로드분)
**문서 ②** `out/ocr_1,3,4,5.json` — test_samples PDF 캡처본 4장

| 앵커 | 문서 ① | 문서 ② | 판정 |
|---|---|---|---|
| `separate_land` (별도등기) | **O** (3줄, p2) | X | 실데이터 검증 가능 |
| `joint_collateral` (공동담보) | **O** (4줄, p4·p5) | X | 실데이터 검증 가능 |
| `pending_application` (신청사건) | X | X | **합성 테스트로만 검증됨** |
| `address` 주소줄 | **O** (4줄) | **O** (4줄) | 양쪽 다 |
| `area` 전용면적 ㎡ | X (표제부 쪽 없음) | **O** (25줄) | 문서 ②로 검증 |
| `doc_title` 등기사항전부증명서 | X (표지 쪽 없음) | **O** (1줄, 말소사항 포함까지) | 문서 ②로 검증 |
| `viewed_at` 열람일시 | **O** (4줄) | X | 문서 ①로 검증 |
| `seizure` 압류 | **O** (6줄) | X | 문서 ①로 검증 |
| `provisional_seizure` 가압류 | **O** (7줄) | X | 문서 ①로 검증 |
| `auction` 경매개시결정 | **O** (2줄) | X | 문서 ①로 검증 |
| `trust` 신탁 | X | X | **합성 테스트로만 검증됨** |
| `jeonse` 전세권 | X | X | **합성 테스트로만 검증됨** |
| `lease_registration` 임차권 | X | X | **합성 테스트로만 검증됨** |
| `mortgage` 근저당권설정 | **O** (7줄) | **O** (4줄) | 양쪽 다 |

→ **합성 테스트로만 검증되는 것 4종**: `pending_application` · `trust` · `jeonse` ·
`lease_registration`. 구현은 다 하되 findings에 이 사실을 명시한다(지시서 A-2 요구).

---

## 00:35 [2] 예상 못 한 발견 — `check_document`의 실제 버그

`ocr_page_1.json`만 저장 시각이 다르다(23:06 vs 나머지 22:44). 조사해 보니:

- `ocr_page_1.json`: 페이지 표식 **없음**, 주소 줄 **없음**, 열람일시 **없음**,
  등기부 키워드(갑구·을구·근저당·순위번호…) **전부 0건**. 즉 **등기부가 아닌 다른 사진**이다.
- `ocr_page_2~5.json`: 표식 `2/5`, `3/5`, `4/5`, `5/5` — 같은 문서(주소 해시 동일).

즉 이 묶음은 **1쪽(표지+표제부)이 빠진 채, 무관한 사진 한 장이 그 자리를 채우고 있다.**

그런데 `check_document()`는 `ok_to_highlight_any=True, money=True, notice=None`을 돌려준다.
이유는 코드를 보면 명확하다:

```python
if markers:
    total = markers[0][1][1]      # 5
    if len(pages) < total:        # len(pages)=5 → 5 < 5 == False → 통과
```

**무관한 사진 1장이 `len(pages)`를 5로 채워 '쪽 누락' 방어를 무력화했다.**
표식이 읽힌 쪽은 4장뿐인데(2,3,4,5) 그 사실이 판정에 쓰이지 않는다.
`발급확인번호`가 이 문서에서는 아예 안 잡히고, 주소도 그 사진엔 없어 `len(addresses)>1`도
False라 두 번째 방어선도 통과한다.

이건 이 방어가 막으려던 **바로 그 시나리오**다 — 빠진 쪽에 말소 근거 행이 있으면
말소된 근저당에 형광펜이 간다. → **작업 F에서 함께 고친다**(§F가 사진 묶음 점검을
다루는 유일한 작업).

**부수 발견**: `_save_raw_ocr`는 `ocr_<파일stem>.json`으로 저장하는데 앱은 사진을 항상
`page_N.jpg`로 보낸다 → **분석할 때마다 이전 원응답이 덮어써진다.** IE 쪽은
`ie_<타임스탬프>.json`이라 남는데 OCR만 사라진다. 작업 E에서 같이 다룬다.

---

## 00:50 [3] 작업 C — IE 재현성 영점 측정 (실호출)

`backend/scripts/measure_ie_reproducibility.py` 신규. 값 대신 **해시**만 기록해
개인정보를 남기지 않는다.

### 결과 ① 단순 문서 (test_samples 5장, 3회) → `docs/ie-reproducibility-2026-07-28.md`

- 응답 17.6 / 18.6 / 25.7초 (중앙값 18.6)
- 배열 길이 **10개 필드 전부 3회 동일**, 항목 지문(원소 해시 집합)도 **전부 동일**
- 스칼라(address·document_title·exclusive_area_sqm) 해시도 동일

→ **같은 입력이면 같은 답이 나온다.** 다만 이 문서는 근저당 2건·압류 0건인 단순 문서라,
복잡 문서로도 재봐야 한다(진행 중).

### 결과 ② 빈 응답 2건의 원인 — **특정됨. IE 흔들림이 아니다.**

`out/ie_*.json` 5건의 스칼라 해시를 비교했다(값 미출력):

| 파일 | address# | document_title# | 배열 합계 |
|---|---|---|---|
| 20:46 | `168a6e` | `36d7e3` | 18 |
| 21:35 | `168a6e` | `36d7e3` | 16 |
| 22:44 | `168a6e` | `36d7e3` | 16 |
| 23:05 | `-` (없음) | **`ca7de3`** | **0** |
| 23:06 | **`50db1a`** | **`9280f8`** | **0** |

빈 응답 2건은 `document_title` 해시가 앞 3건과도, 서로 간에도 **다르다**.
즉 **입력 문서 자체가 달랐다** — 같은 문서에 IE가 빈 답을 준 것이 아니다.
23:06 건은 `ocr_page_1.json`(등기부 아닌 사진)의 저장 시각과 정확히 맞는다.

### 결과 ③ 18 vs 16 차이의 정체 — `provisional_seizures`

항목별 `rank_number` 분포를 세어 보니:

| 배열 | 순위번호 분포 (3개 파일 합산) |
|---|---|
| `seizures` | 2×3, 4×3, 7×3, 8×3, 9×3 |
| `provisional_seizures` | 2×3, 4×**1**, 7×3, 8×3, 9×**1** |

→ **IE가 압류와 가압류에 같은 순위번호(2,4,7,8,9)를 중복 배정한다.**
1회차만 4번·9번을 `provisional_seizures`에도 넣었고(=5건), 2·3회차는 빼서 3건이 됐다.

같은 순위번호를 OCR 등기목적으로 확인하면:
`가압류 = {2, 7, 8}` / `압류 = {4, 9}` (3번은 항목 경계가 붙어 혼재).
**2·3회차의 `provisional_seizures` = {2,7,8}은 OCR이 읽은 가압류 집합과 정확히 일치한다.**

→ 이건 이번 세션 최대 성과 후보다. **IE 단독으로는 압류/가압류를 안정적으로 못 가른다.
등기목적 텍스트를 읽는 두 번째 경로가 그 구분을 준다.** 작업 B(3경로)의 존재 이유가
가설이 아니라 실측으로 뒷받침됐다. → `docs/presentation-material.md`에 기록.

---

## 01:20 [4] 작업 A — 형광펜 15종 확장 (커밋 `c4bd0c9`)

- `_SPECS` 표 하나로 종류·순서·문구·출처를 모은다. 새 종류는 여기 한 줄.
- **2앵커 도입** — 근거는 위 [1]의 실측(압류 청구금액 3/6, 등기목적 8/8 같은 줄).
- 실데이터로 확인: 12건 표시(주소1·별도등기3·이름1·압류1·경매1·공동담보4·열람일1).
  **IE가 압류라 한 순위 2·7·8을 OCR 등기목적이 '가압류'라며 거부**했다 — 교차검증이
  코드 안에서 이미 일어나고 있었다. 그걸 화면으로 끌어내는 게 작업 B다.
- 박스 폭 버그 하나를 테스트가 잡았다: 2앵커 키워드를 줄 전체에서 찾으니 등기원인 칸의
  같은 단어까지 잡아 **9.9% → 45.1%로 번졌다.** 등기목적 칸으로 제한해 고쳤다.

## 02:10 [5] 작업 B — provider 추상화·3경로·교차검증 (커밋 `42bed06`)

decisions.md를 읽다가 **[2026-07-22] LLM 다원화** 항목을 발견했다 — provider 추상화는
이미 설계·승인돼 있었고 `enable_thinking=false` 이슈도 그때 기록돼 있었다.
**내 "발견"이 아니라 재현이다.** 커밋 메시지와 decisions 항목에 그렇게 적었다.

실호출로 새로 알아낸 것:
- FriendliAI serverless의 EXAONE 계열은 `LGAI-EXAONE/K-EXAONE-236B-A23B` **하나뿐**
  (`GET /serverless/v1/models` 확인). 예전 이름은 404.
- 그 모델에 등기부 구조화를 시키면 **사고 6,143~8,717자**를 쓰고 `content`가 빈 문자열로
  온다(3/3 전부 실패). `enable_thinking=false`로 끄면 즉시 정상 — [2026-07-22] 기록의 재현.

## 02:40 [6] 작업 F·E — 사진 순서 정렬 + 보관 상한

- `_resolve_order`의 조건 넷이 이 기능의 본체다. 하나라도 못 채우면 예전처럼 보류.
- 좌표는 **원래 업로드 인덱스로 복원**. 내부는 문서 순서, 화면은 업로드 순서.
- [2]에서 찾은 `check_document` 버그를 여기서 고쳤다(장수 → 읽힌 쪽 번호).
- `artifacts.py`: `out/runs/<회차>/`로 IE·OCR을 같이 남기고 최근 5회분만 유지.
  **운영 모드에서는 저장 자체가 없다**(테스트로 봉인).

## 03:30 [7] 추가 지시 도착 — Document Parse 조사 (커밋 `38c3ac2`)

세션 도중 새 조사 과제가 왔다. 진행 중이던 작업을 커밋으로 끊고 착수했다.

`test_samples` 5장 실호출. **판단: 부분 대체.**
- **좌표는 대체 불가** — 표 하나당 사각형 하나(페이지의 20~61%), `words` 필드 없음.
- **구조는 확실히 낫다** — 접힌 셀이 한 셀에, 순위번호 칸이 깨끗, `footer` 카테고리.
- `.env`의 `LAYOUT_SOURCE`로 **구조화 경로의 입력 텍스트만** 고르게 했다.

선택 과제(같은 문서·같은 하네스로 두 입력 비교):

| provider | ocr_layout | document_parse |
|---|---|---|
| Solar 항목수 일치율 | 96% | **100%** |
| EXAONE 성공 | 2/3 | **3/3** |

비교를 준비하다 **함정 하나를 스스로 잡았다**: `ocr_page_*`(서초동)와 `dp_*`(test_samples)는
서로 다른 문서다. 그대로 비교했으면 숫자가 통째로 무의미했다. 같은 문서로 맞추려고
빠져 있던 `ocr_2.json`을 1회 호출로 채우고, IE 대조 기준도 `--ie`로 명시 지정하게 했다.

## 04:10 [8] 작업 D — 결정 이관 + 새 절

decisions.md에 9개 항목 추가. **새 절 "고려했으나 하지 않은 것과 그 이유" 신설**(13건).
findings의 "채택 미확정" 딱지 제거 + §10(후속 세션에서 더해진 사실) 추가.
plan.md에 E-2b 위치와 그 이유 기록. CLAUDE.md의 `data-contract.md` 오기 정정.

## 04:40 [9] 자기 공격 — **내 오늘 설계에서 구멍을 찾았다**

§9의 "말소된 압류에 좌표가 가는 경로를 스스로 공격해 찾아라"를 하다가,
**오늘 내가 만든 카브아웃이 그 구멍이었다.**

처음엔 "2앵커 종류는 금액과 무관하니 표시 보류에서 빼자"고 했다. 그런데 보류 트리거가 둘이다:

| 트리거 | 성격 | 무엇을 막아야 하나 |
|---|---|---|
| **쪽 누락** | 추정 ("빠진 쪽에 말소 행이 있을 수도") | 금액 종류만 |
| **말소 미결** | **사실** ("올린 쪽에 말소 행이 있는데 대상을 못 찾았다") | **전부** |

두 번째는 "우리 말소 판정이 지금 깨져 있다"는 구체적 증거인데, 내 카브아웃이 그 경우에도
압류를 칠하게 두고 있었다. → `cancel_reliable` 축을 분리해 막았다.
전 종류를 한 번에 훑는 파라미터 테스트를 함께 깔았다.

**두 트리거를 다르게 다루는 근거**(코드 주석에도 남김): 금액 종류는 "집에 잡힌 빚 5억"처럼
**사용자가 그대로 믿는 숫자**를 각인시키지만, 2앵커 종류는 금액이 없고 문구가 스스로
교정한다("풀렸는지 등기부에서 직접 확인한 뒤에 계약하세요"). 쪽 한 장 잘렸다고 가장
심각한 위험까지 화면에서 지우면 그건 위험을 숨기는 쪽이다.

## 05:00 [10] 리뷰·기록

- 서브에이전트 4종 투입(지수·서연·rule-auditor·gap-checker).
- `docs/presentation-material.md` 신규 — 숫자 있는 사례 6건.
- `docs/team-setup.md`에 국내 LLM 3사 키 설정 절 추가.
- **실기기 스크린샷은 못 찍었다** — `adb devices`에 기기가 없다(연결 안 됨).
  A-3.4가 요구한 육안 확인이 미완이다. 아침 보고서에 "사람이 확인해야 할 것" 1번으로 올렸다.

## 05:40 [11] 리뷰 도착 — **세 리뷰어가 같은 버그를 독립적으로 찾았다** (커밋 `a6ae6af`)

지수·서연·gap-checker가 각각 찾은 것이 같았다:
**내가 오늘 만든 안내 문장들이 화면에 아예 나오지 않고 있었다.**

앱은 `checkedNotes` 중 `['표시하지 않았','찾지 못했','생략했']`이 든 문장만 그린다.
오늘 만든 상한 고지·교차검증 일치·자동 정렬 고지가 **전부 탈락**하고 있었다.
결과적으로 **교차검증의 "확인했다"는 절반은 안 보이고 "갈렸다"는 절반만** 보였다.
서연의 말이 정확하다 — *"그건 '우리 꼼꼼해요'가 아니라 '우리도 몰라요'로 읽혀요."*

rule-auditor는 **판정 수치 위반 0건 / LLM→판정 경로 0건**을 확인하고 두 가지를 잡았다:
- 쪽이 빠진 걸 알면서 "빚은 없었어요"라고 단정 → **보수편향 역전**
- 설명 생성 temperature가 0.3 → 0.1로 **조용히 회귀**(기록값과 불일치)

그리고 내가 쓴 교차검증 문구가 **거짓말할 뻔했다**: "위험 계산은 더 엄격한 쪽 기준으로
했어요"라고 썼는데, 판정은 언제나 IE 기준이고 **IE가 더 적게 잡는 경우가 있다.**
→ 사실대로 분기했다.

## 06:20 [12] 2차 처리 — "판단이 필요 없는 것"을 마저 (커밋 `07f611a`)

미뤄 뒀던 리뷰 지적 중 설계 결정이 아닌 것을 처리했다. 가장 큰 것:

**gap-checker #8 — 사진 정렬이 LLM 경로에 반영되지 않고 있었다.**
두 번째 경로는 업로드 순서를 그대로 읽고, 정렬 판단은 그 **뒤에** 계산됐다.
즉 **LLM만 뒤섞인 문서를 읽었고**, 그 결과가 "빚은 문서 판독 3건 / 사진 판독 1건" 같은
무서운 불일치로 사용자에게 나갔다 — **원인이 등기부가 아니라 우리 파이프라인이었다.**

그 밖에: DP 부분 실패 시 반쪽 문서 사용 / 같은 쪽 사진 두 장이 모든 점검 통과 /
카드 첫 문장이 행동이 아니었던 것(13종 재작성) / 용어 5종 / `api-contract.md` 갱신.

실데이터 표시가 **12건 → 7건**으로 정리됐다(텍스트 3종 중복 제거 효과).

## 07:00 [13] 마지막 구멍 — 화면이 통째로 비는 경로 (커밋 `16fcad3`)

실데이터로 사용자 문구를 한 줄씩 필터에 통과시켜 보다가 찾았다.
`"압류·가압류·신탁 같은 표시가 N건 있어요 — 리포트를 꼭 보세요"`가 어느 표식에도
안 걸린다. 표시가 0건인데 이 경고까지 사라지면 **화면이 통째로 빈다.**
**가처분은 아직 매칭 규칙이 없어 항상 이 경로다.**

→ 문장 끝을 표식에 맞춰 화면에 나가게 했다.

## 07:20 [14] 정리 — RESUME·cleanup-tracker (커밋 `f976b21`)

- `RESUME.md`가 07-27 세션 내용이라 이어받기에 오히려 방해였다. 지우는 대신 현재 상태로
  덮어썼다 — **남은 항목 7건이 실제로 있어** 빈 파일보다 낫다.
- cleanup-tracker에 이번 산출물 9종 등록. **정리 대상이 아닌 것도 왜 아닌지 적었다**
  (다음 사람이 지우려다 멈출 수 있게).
- `design_handoff_registry_viewer/`·`night-log.md`는 **보존 결정**으로 상태 갱신 —
  각각 유일한 테스트 사본·발표 원재료가 됐다.

---

## 이 세션의 교훈 (다음 세션이 읽을 것)

1. **`decisions.md`를 먼저 읽어라.** EXAONE `enable_thinking=false`와 FriendliAI 429는
   [2026-07-22] 항목에 이미 있었다. 재발견에 30분을 썼다.
2. **문장을 만들었으면 화면에 나오는지 확인해라.** 백엔드에서 "침묵 금지"라고 써 놓고
   앱 필터에서 침묵하고 있었다. 리뷰어 3인이 각각 찾아낼 만큼 컸다.
3. **비교 실험은 입력이 같은지부터 확인해라.** `ocr_page_*`와 `dp_*`가 서로 다른 등기부라
   그대로 비교했으면 숫자가 통째로 무의미했다.
4. **내가 방금 만든 것도 공격 대상이다.** 오늘 만든 2앵커 카브아웃이 그날의 구멍이었다.
