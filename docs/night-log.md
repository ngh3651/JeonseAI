# 밤샘 작업 로그 — OCR 하이라이트 (2026-07-27 야간)

> 이 문서는 **작업하면서 그때그때 append**하는 실시간 기록이다. 마지막에 몰아서 요약한
> 문서가 아니다. 나중에 읽는 사람이 그 자리에 없었어도 무슨 일이 있었는지 재구성할 수
> 있도록 결정·에러·포기 지점을 전부 남긴다.
>
> 브랜치: `feat/ocr-highlight` / 시작 커밋: `4692d12`
> 최종 보고서는 `docs/morning-report.md`, 중단 대비 요약은 `docs/RESUME.md`.

---

## 00:00 세션 시작 — 사전 조사

**목표**: 사용자가 찍은 등기부 사진 위에 위험·확인 항목 위치를 형광펜처럼 표시.
실기기(SM S931N)에서 눈으로 볼 수 있는 상태까지.

**금지 사항 확인 (지시서)**: git push 금지 / 규칙 엔진 변경 금지 / IE↔OCR 교차검증으로
등급 변경 금지 / decisions.md·plan.md 수정 금지 / backend/out/ 커밋 금지 / 기존 백엔드
테스트 61건 유지 / 사진 영구 저장 금지.

**읽은 것**:
- `backend/scripts/test_ocr_coords.py` (863줄) — 라운드 0 수정 대상
- `backend/app/services/extraction.py` — IE 호출 패턴(키 로딩·에러 클래스·상태코드
  한국어 매핑·타임아웃·로깅). OCR 서비스가 그대로 따라야 할 원본
- `backend/app/routers/reports.py` — `/api/analyze` 진입점
- `backend/app/services/report_builder.py` — `analyze()`가 추출→판정→설명→저장 순서.
  **여기에 OCR 병렬 호출을 끼워 넣어야 한다**
- `backend/app/schemas/contract.py` — Report/Evidence 계약 스키마. 좌표는 여기에
  **선택적 필드로만** 추가
- `backend/app/schemas/internal.py` — RegistryExtract/RuleVerdict

**확인된 구조적 사실**:
- `report_builder.analyze()`가 `extraction.extract_registry(images)` 하나만 호출한다.
  OCR을 병렬로 넣으려면 이 지점을 `ThreadPoolExecutor`로 감싸야 한다(FastAPI 핸들러가
  동기 `def`라 이미 스레드풀에서 돌고 있음 — 안에서 스레드 2개 쓰는 건 안전).
- `backend/out/`에 이전 세션의 OCR 응답 JSON 4장(`ocr_1/3/4/5.json`)이 이미 있다.
  → 라운드 0 재검증과 라운드 1 pytest 픽스처 둘 다 **API 호출 0회**로 가능하다.
- `test_samples/`에 1~5.png 5장. 단 OCR JSON은 1,3,4,5만 있음(2.png 없음).

## 00:35 라운드 0 시작 — 검증 스크립트 수정

**하려는 것**: `backend/scripts/test_ocr_coords.py`의 알려진 문제 3건 수정 + 항목 단위
그룹화 추가. 여기서 만든 로직이 라운드 1의 백엔드 매칭으로 승격되므로, 대충 고치면
백엔드까지 같이 틀린다.

**먼저 좌표를 실측했다** (`--from-json`, API 호출 0회). 3·4.png의 word별 x범위를 전부
찍어보니 표 컬럼이 x좌표로 깨끗하게 갈렸다:

```
3.png 헤더 L2: 순위번호@42-101 | 등기목적@145-226 | 접수@302-346 | 등기원인@405-487 | 권리자및기타사항@616-752
3.png L3  데이터: 1@67-76 | 소유권보존@117-192 | 2002년9월18일@269-367 | 소유자@516-560 | 소유자A@576-619 | ○○○○○○-○******@634-741
3.png L19 데이터: 1@117-125 | 중@133-147 | 일부(4분의1),@156-251 | 제50495호@271-338 | 매매@394-422 | 소유자D@531-575 | ○○○○○○-○******@589-696
```

**여기서 문제 2건의 진짜 원인이 드러났다.**

### 원인 1 — 순위번호 오검출 (기존 코드가 L19를 '순위 1'로 잡음)
기존 `Line.rank_no`는 "줄 맨 앞 word가 숫자면 순위번호"로 판정한다. 그런데 L19의 맨 앞
word `1@117-125`는 순위번호가 아니라 **등기목적 칸에서 접힌 "…2분의 / 1 중 일부(4분의1)"의
분모 1**이다. 진짜 순위번호는 x중심이 66~77인데 이건 121이다.

→ **결정: 순위번호는 "줄 맨 앞"이 아니라 "순위번호 컬럼 x밴드 안"으로 판정한다.**
   밴드는 헤더 줄의 `순위번호` word 박스(42~101)를 그대로 쓴다. 실측 순위번호 중심은
   전부 66~72로 밴드 한가운데에 들어오고, 가짜(121)는 밖으로 떨어진다.
   - 버린 선택지 ⓐ "맨 앞 word + 짧은 텍스트" 휴리스틱 → L19가 정확히 그 조건을 만족해서
     못 거른다.
   - 버린 선택지 ⓑ 하드코딩된 x임계값(예: x<100) → 페이지마다 스캔 여백이 다르면 깨진다.
     헤더 word 기준이면 페이지마다 자동으로 따라간다(3.png는 42-101, 4.png는 38-98로 실제
     다르다).

### 원인 2 — 말소 행이 두 줄로 갈라지는 것은 '줄 묶기 실패'가 아니었다
4.png L12 `3 | 1번근저당권설정등 | 2005년6월29일 | 2005년6월29일`
4.png L13 `기말소 | 제45409호 | 해지`
줄 묶기는 정상이다. 표의 **등기목적 칸 안에서 텍스트가 2줄로 접힌 것**이고, 그 사이에
접수·등기원인 칸 글자가 끼어 있다. 그래서 줄 단위로 이어붙이면
`31번근저당권설정등2005년6월29일2005년6월29일` + `기말소제45409호해지` 가 되어
"근저당권설정등기말소"가 **날짜에 의해 끊긴다**.

→ **결정: 말소 패턴은 '항목 안의 등기목적 컬럼 word만' 읽기순으로 이어붙여 찾는다.**
   등기목적 밴드 = (순위번호 헤더 x1, 접수 헤더 x0) = 4.png 기준 (98, 297).
   중심좌표로 걸러보면 `1번근저당권설정등`(177) `기말소`(136)만 남고 날짜(314)는 빠진다
   → `1번근저당권설정등기말소` 완성.
   - 버린 선택지 ⓐ 줄 tol을 키워 L12·L13을 한 줄로 묶기 → 다른 항목까지 뭉개져서 순위
     구분이 무너진다. 부작용이 이득보다 크다.
   - 버린 선택지 ⓑ 항목 전체 텍스트에서 `\d+번.*?말소` 를 느슨하게(dotall) 찾기 → 날짜를
     건너뛰긴 하지만 **다른 항목의 '말소'까지 끌어올 위험**이 있다. 말소 오판정은
     "이미 없어진 빚"을 유효로 보이게 하거나 그 반대라 치명적이라 쓰지 않는다.

### 원인 3 — 이름 '매매소유자D' 은 문자 단위 정규식의 한계
기존 `_NAME_WITH_RRN`는 줄을 **공백 없이** 이어붙인 문자열에 `[가-힣]{2,10}` 을 물려서
`매매`+`소유자D`을 한 덩어리로 삼켰다. 그런데 실제 word 배열은
`['…','제50495호','매매','소유자D','○○○○○○-○******']` 로 **이미 '소유자D'이 독립 word**다.

→ **결정: 이름 추출을 문자 단위에서 word 단위로 바꾼다.**
   ⑴ `\d{6}-[\d*]{7}` 형태의 등록번호 word를 먼저 찾고 ⑵ 바로 앞 word를 이름 후보로
   삼는다 ⑶ 후보 앞에 붙은 역할어(소유자/공유자/근저당권자…)·등기원인어(매매/설정계약/
   해지…)를 떼어낸다. bbox도 그 word 하나만 쓰므로 181px → 43px로 좁혀진다.

### 덤으로 확인된 것 — 개인/법인 구분 규칙
- 개인: `○○○○○○-○******` (뒤 7자리가 **마스킹**)
- 법인: `110111-0015671` (뒤 7자리가 **숫자**)
→ 뒷자리가 숫자인지 별표인지만 보면 개인/법인이 갈린다. 이름 글자수 규칙(개인 2~4자)도
  여기에 맞춰 나눠 적용한다.

## 01:20 라운드 0 결과

**구현**: 레이아웃 해석 로직을 **`backend/app/services/ocr_layout.py` 새 모듈로** 만들고,
검증 스크립트가 그것을 import 하도록 바꿨다.
- **왜 모듈로 뺐나**: 지시서는 "라운드 0의 로직이 라운드 1의 백엔드 매칭으로 승격된다"고
  했다. 스크립트에 짜고 나중에 백엔드로 복사하면 **두 벌이 갈라져** "스크립트에선 되는데
  서버에선 안 되는" 상황이 온다. 처음부터 한 벌로 두고 스크립트가 그것을 검증하게 했다.
- 스크립트에서 삭제한 것: `Word`/`Line` 클래스, `parse_words`, `group_lines`,
  `_clean_name`, `_NAME_WITH_RRN`, `_OWNER_KEYWORD`, `_ROLE_WORDS`, `_RANK_NO`,
  `Line.rank_no` 프로퍼티.

**재검증 결과 (`--from-json out`, API 호출 0회 / 크레딧 0원)**

이름 5건 — **전부 정확**:
```
[개인] '소유자A'  bbox (576,227)-(619,243)  폭 43px
[개인] '소유자B'  bbox (531,422)-(574,438)  폭 43px
[개인] '소유자C'  bbox (531,500)-(575,516)  폭 44px
[개인] '소유자D'  bbox (531,597)-(575,613)  폭 44px   ← 과거 '매매소유자D' 181px
[개인] '소유자E'  bbox (531,714)-(574,730)  폭 43px   ← 과거 '매매소유자E'
[법인] '주식회사하나은행' / '주식회사우리은행'
```
개인/법인 구분도 정상(마스킹 여부로 판별).

항목 그룹화 — 갑구 5·을구 4 항목이 정확히 잡혔고 **말소가 근거와 함께** 붙었다:
```
✗ [을구 순위 1] 근저당권설정 금36,000,000원 / 주식회사하나은행
    말소  근거 4:L12-13 의 '1번근저당권설정등기말소'   위치 3:L30-33,4:L2-4
✗ [을구 순위 2] 근저당권설정 금75,600,000원 / 주식회사우리은행
    말소  근거 4:L14-15 의 '2번근저당권설정등기말소'   위치 4:L5-11
· [을구 순위 3] 1번근저당권설정등기말소  (말소근거행)
· [을구 순위 4] 2번근저당권설정등기말소  (말소근거행)
```
을구 1번이 `3:L30-33,4:L2-4` 로 **페이지를 걸쳐 이어졌다** — 금액은 3.png, 하나은행은
4.png. 설계 전제(항목은 페이지 경계에서 닫히지 않는다)가 실제로 맞았다.

**가장 중요한 교차 확인**: `out/marked_4.png`를 눈으로 보니 원본 등기부에서 **빨간
취소선이 그어진 행**(을구 1·2번)과 우리가 텍스트만으로 말소 판정한 행이 **정확히
일치**한다. 반대로 을구 3·4번(말소를 기록한 행)에는 취소선이 없고, 우리도 이것을
'말소근거행'으로 따로 분류했다. 선 정보 없이 텍스트만으로 말소를 가려낼 수 있음이
시각적으로 증명됐다.

### 예상과 달랐던 것 ① — 표제부·매매목록 오탐
- **기대**: 항목 그룹화가 갑구·을구만 건드릴 것
- **실제**: `[매매목록 순위 2]`, `[표제부 순위 2]`가 **'말소근거행'으로 오탐**됨
- **원인**: 4.png 마지막 항목이 열린 채 1.png로 넘어가면서 1.png 표지의
  `등기사항전부증명서(**말소**사항 포함)` 문구를 항목 안으로 삼켰다. 그 안에 '말소'가
  있으니 말소 행으로 본 것이다.
- **대응** (2가지 동시에):
  ⑴ `_DOC_PREAMBLE` 정규식 추가 — 등기사항전부증명서/고유번호/제출용/열람일시/
     발급확인번호/인터넷등기소/수수료/관할등기소 가 있는 줄은 **어떤 항목에도 붙이지 않는다**.
  ⑵ 말소 판정 자체를 **갑구·을구에서만** 수행(`RIGHTS_SECTIONS`). 표제부(건물 표시)와
     매매목록은 권리 항목이 아니라 부가 정보다.
- **결과**: 오탐 2건 사라짐. 말소근거행 4건 → 2건(진짜 을구 3·4번만).

### 예상과 달랐던 것 ② — 기존 테스트 수
- **기대**: 지시서에 적힌 61건
- **실제**: `pytest -q` → **86 passed**
- **원인**: 지시서 작성 이후 테스트가 늘어난 것으로 보임(확인 불가)
- **대응**: 기준선을 **86건**으로 잡는다. "기존 테스트가 계속 통과한다"는 요구는 그대로
  지킨다. 라운드 1 이후에도 86건 + 신규가 모두 통과해야 한다.

### 남은 잡음 (고치지 않고 넘어감)
표제부/매매목록 항목의 `등기목적` 텍스트가 지저분하다
(`[표제부 순위 2] 2006-150금860,000,000원`). 표지·꼬리말 페이지에는 등기 표가 없어
컬럼 밴드가 직전 페이지 것으로 남아 있기 때문이다.
- **왜 안 고치나**: 하이라이트 대상은 **갑구 이름 + 을구 금액**뿐이라 표제부/매매목록
  항목은 애초에 매칭 후보에 들어가지 않는다. 화면에 영향이 없다.
- **다시 하려면**: `build_items`에서 페이지가 바뀔 때 그 페이지에 표 헤더가 없으면
  컬럼 밴드를 무효화하면 된다. 다만 그러면 을구 1번의 페이지 걸침(4.png L2-4)이 깨지므로
  "헤더 없는 페이지"와 "헤더 있는 이어지는 페이지"를 구분해야 한다 — 이득 대비 복잡하다.
